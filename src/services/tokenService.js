class TokenService {
  constructor(repo) {
    super(repo);
  }

  getAccessToken = async () => {
    // POST parameters to the token URI, using the developer's client ID and secret with HTTP basic authorization:
  };
}
